# Example environment variables for the drd4 project
# Copy this to a file named .env and fill in secrets (this repo ignores .env)

# NCBI API key (you can either set this directly here or use `env:VARNAME` or `file:/path` in config.json)
# e.g. NCBI_API_KEY=my_api_key_here
NCBI_API_KEY=

# Cache TTL in seconds (default 604800 = 7 days)
NCBI_CACHE_TTL_SECONDS=604800

# Input and output defaults (can be overridden in config.json)
INPUT_FASTA=drd4-tdah.fasta
OUTPUT_JSON=drd4-database.json

# Logging
LOG_FILE=
LOG_LEVEL=info

# Optional: path to a local cache file for NCBI results
NCBI_CACHE_PATH=
